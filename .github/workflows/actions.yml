name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello world"
          echo "Hello github user. Welcome to github actions"
      - name: Print goodbye
        run: |
          echo "Good bye"
          echo "Exiting..."
